# i18n (English + 한국어) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** muxbar 의 모든 user-facing string 을 영어 + 한국어 둘 다 제공. 첫 실행 시 macOS system locale 자동 감지, Settings 에서 Auto/English/한국어 picker 로 manual override + 자동 재시작.

**Architecture:** 모든 string 을 `Core` 모듈의 `Resources/{en,ko}.lproj/Localizable.strings` 에 통합. 타입세이프 `enum L` 로 IDE 자동완성 + 오타 방지. `LocaleService` 가 `UserDefaults` 의 `muxbar.language` (`auto`/`en`/`ko`) 와 시스템 `AppleLanguages` override 를 관리. 언어 변경은 alert 후 `NSWorkspace.openApplication` 으로 새 인스턴스 띄우고 `NSApp.terminate`.

**Tech Stack:** Swift 5.9, SwiftPM resources (`.process("Resources")`), `Bundle.module`, `NSLocalizedString`, SwiftUI `Picker`/`alert`, `NSWorkspace`, `UserDefaults`.

**Spec:** `docs/specs/2026-05-09-i18n-design.md`

---

## File Structure

```
Sources/
├── Core/
│   ├── Resources/                       (신규)
│   │   ├── en.lproj/Localizable.strings
│   │   └── ko.lproj/Localizable.strings
│   └── I18n.swift                       (신규)
├── Features/
│   ├── KeepAwake/KeepAwakeMenuItem.swift     (수정: string → L.x)
│   ├── ClosedLid/ClosedLidMenuItem.swift     (수정)
│   ├── Settings/SettingsMenu.swift           (수정: 치환 + Language picker)
│   ├── Templates/NewSessionMenu.swift        (수정)
│   └── SessionList/{SessionListView,SessionRowView}.swift  (수정)
└── MuxBarApp/
    ├── MuxBarApp.swift                  (수정: header / Quit / tooltip 치환 + applyAtLaunch)
    └── AppState.swift                   (수정: LocaleService instance)

Tests/CoreTests/
└── I18nTests.swift                      (신규)

Package.swift                            (수정: Core target resources)
```

테스트 환경 주의: 사용자 머신 Xcode.app 미설치 시 `swift test` 가 XCTest 모듈 못 찾는 케이스. `swift build -c release` 만 통과 + 코드 리뷰로 갈음 가능. 이미 다른 plan 의 동일 제약.

---

## Task 1: SPM Resources 인프라 + en.lproj 초기 셋업

**Files:**
- Create: `Sources/Core/Resources/en.lproj/Localizable.strings`
- Modify: `Package.swift`

목적: `Core` target 의 resources 등록 → SPM 이 `Bundle.module` 자동 생성. en.lproj 에 모든 키 작성. 이번 task 까지 끝나면 `Bundle.module` 빌드 시점에 노출됨.

- [ ] **Step 1: en.lproj/Localizable.strings 작성**

`Sources/Core/Resources/en.lproj/Localizable.strings` (신규):

```
// menu (top level)
"menu.keepAwake"          = "Keep Awake";
"menu.closedLid"          = "Closed-lid mode";
"menu.newSession"         = "New Session";
"menu.settings"           = "Settings";
"menu.quit"               = "Quit muxbar";
"menu.attach"             = "Attach";
"menu.preview"            = "Preview";
"menu.kill"               = "Kill";
"menu.openTemplates"      = "Edit Templates…";
"menu.reloadTemplates"    = "Reload Templates";

// closed-lid duration popover
"closedLid.duration"      = "Duration";
"closedLid.duration.30m"  = "30 min";
"closedLid.duration.1h"   = "1 hour";
"closedLid.duration.4h"   = "4 hours";
"closedLid.duration.8h"   = "8 hours";
"closedLid.duration.inf"  = "∞ (until manual off)";

// closed-lid state
"closedLid.state.off"     = "OFF";
"closedLid.state.onInf"   = "ON · ∞";
"closedLid.state.onTimer" = "ON · %@";
"closedLid.subtitle"      = "sleep blocked (system-wide)";

// keep awake state
"keepAwake.state.on"      = "ON";
"keepAwake.state.off"     = "OFF";
"keepAwake.state.external" = "ON (external)";
"keepAwake.state.both"    = "ON (muxbar + external)";
"keepAwake.externalPrefix" = "external: %@";

// settings
"settings.openAtLogin"            = "Open at Login";
"settings.openAtLogin.unavailable" = "Open at Login (install .app to enable)";
"settings.closedLidSection"        = "Closed-lid mode";
"settings.keepDisplayAwake"        = "Keep display awake";
"settings.preventScreenSaver"      = "Prevent screen saver";
"settings.language"                = "Language";
"settings.language.auto"           = "Auto";
"settings.language.en"             = "English";
"settings.language.ko"             = "한국어";
"settings.relaunch.title"          = "Restart muxbar?";
"settings.relaunch.body"           = "Language changes apply after restart.";
"settings.relaunch.confirm"        = "Restart";
"settings.relaunch.cancel"         = "Cancel";

// errors
"error.tmuxNotConnected"  = "tmux not connected — connect first";

// menubar tooltip
"tooltip.idle"            = "muxbar — tmux session manager";
"tooltip.keepAwake"       = "muxbar — Keep Awake active (caffeinate running)";
"tooltip.closedLid"       = "muxbar — Closed-lid mode active (system sleep blocked)";

// app
"app.name"                = "muxbar";
```

- [ ] **Step 2: Package.swift 의 Core target 에 resources 추가**

`Package.swift` 의 `.target(name: "Core", ...)` 정의 수정 — 기존:

```swift
.target(
    name: "Core",
    dependencies: ["MuxLogging", .product(name: "Logging", package: "swift-log")],
    swiftSettings: [.enableExperimentalFeature("StrictConcurrency")]
),
```

→ 변경:

```swift
.target(
    name: "Core",
    dependencies: ["MuxLogging", .product(name: "Logging", package: "swift-log")],
    resources: [
        .process("Resources"),
    ],
    swiftSettings: [.enableExperimentalFeature("StrictConcurrency")]
),
```

- [ ] **Step 3: 빌드 검증**

```bash
swift build -c release 2>&1 | tail -3
```
Expected: `Build complete!`. SPM 이 Resources/ 를 인식하고 Bundle.module 자동 생성 → Core 모듈에서 `Bundle.module` accessor 사용 가능.

- [ ] **Step 4: 커밋**

```bash
git add Sources/Core/Resources/en.lproj/Localizable.strings Package.swift
git commit -m "$(cat <<'EOF'
feat(Core): SPM resources + en.lproj 초기 키 셋업

Core target 에 .process("Resources") 추가 → Bundle.module 자동 생성.
en.lproj/Localizable.strings 에 menu / closedLid / keepAwake / settings /
error / tooltip 영역 키 작성.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: ko.lproj + enum L + 키 일치 테스트

**Files:**
- Create: `Sources/Core/Resources/ko.lproj/Localizable.strings`
- Create: `Sources/Core/I18n.swift`
- Create: `Tests/CoreTests/I18nTests.swift`

목적: 한국어 번역 + 타입세이프 lookup 키 + 모든 key 가 양쪽 lproj 에 존재하는지 자동 검증.

- [ ] **Step 1: ko.lproj/Localizable.strings 작성**

`Sources/Core/Resources/ko.lproj/Localizable.strings` (신규):

```
// menu (top level)
"menu.keepAwake"          = "Keep Awake";
"menu.closedLid"          = "Closed-lid mode";
"menu.newSession"         = "새 세션";
"menu.settings"           = "설정";
"menu.quit"               = "muxbar 종료";
"menu.attach"             = "연결";
"menu.preview"            = "미리보기";
"menu.kill"               = "종료";
"menu.openTemplates"      = "템플릿 폴더 열기…";
"menu.reloadTemplates"    = "템플릿 다시 읽기";

// closed-lid duration popover
"closedLid.duration"      = "지속 시간";
"closedLid.duration.30m"  = "30분";
"closedLid.duration.1h"   = "1시간";
"closedLid.duration.4h"   = "4시간";
"closedLid.duration.8h"   = "8시간";
"closedLid.duration.inf"  = "∞ (수동 OFF 까지)";

// closed-lid state
"closedLid.state.off"     = "OFF";
"closedLid.state.onInf"   = "ON · ∞";
"closedLid.state.onTimer" = "ON · %@";
"closedLid.subtitle"      = "sleep 차단 (시스템 전역)";

// keep awake state
"keepAwake.state.on"      = "ON";
"keepAwake.state.off"     = "OFF";
"keepAwake.state.external" = "ON (외부)";
"keepAwake.state.both"    = "ON (muxbar + 외부)";
"keepAwake.externalPrefix" = "외부: %@";

// settings
"settings.openAtLogin"            = "시작 시 자동 실행";
"settings.openAtLogin.unavailable" = "시작 시 자동 실행 (.app 설치 후 활성화)";
"settings.closedLidSection"        = "Closed-lid mode";
"settings.keepDisplayAwake"        = "화면 켜둠";
"settings.preventScreenSaver"      = "화면 보호기 차단";
"settings.language"                = "언어";
"settings.language.auto"           = "자동";
"settings.language.en"             = "English";
"settings.language.ko"             = "한국어";
"settings.relaunch.title"          = "muxbar 재시작?";
"settings.relaunch.body"           = "언어 변경은 재시작 후 적용됩니다.";
"settings.relaunch.confirm"        = "재시작";
"settings.relaunch.cancel"         = "취소";

// errors
"error.tmuxNotConnected"  = "tmux 연결되지 않음 — 먼저 연결하세요";

// menubar tooltip
"tooltip.idle"            = "muxbar — tmux 세션 관리";
"tooltip.keepAwake"       = "muxbar — Keep Awake 활성 (caffeinate 실행 중)";
"tooltip.closedLid"       = "muxbar — Closed-lid mode 활성 (시스템 sleep 차단)";

// app
"app.name"                = "muxbar";
```

- [ ] **Step 2: I18n.swift 작성 — enum L 의 모든 키**

`Sources/Core/I18n.swift` (신규):

```swift
import Foundation

/// 타입세이프 user-facing string 키. 모든 NSLocalizedString lookup 의 단일 진입점.
public enum L {
    // MARK: menu
    public static var menuKeepAwake: String       { lookup("menu.keepAwake") }
    public static var menuClosedLid: String       { lookup("menu.closedLid") }
    public static var menuNewSession: String      { lookup("menu.newSession") }
    public static var menuSettings: String        { lookup("menu.settings") }
    public static var menuQuit: String            { lookup("menu.quit") }
    public static var menuAttach: String          { lookup("menu.attach") }
    public static var menuPreview: String         { lookup("menu.preview") }
    public static var menuKill: String            { lookup("menu.kill") }
    public static var menuOpenTemplates: String   { lookup("menu.openTemplates") }
    public static var menuReloadTemplates: String { lookup("menu.reloadTemplates") }

    // MARK: closed-lid
    public static var closedLidDuration: String      { lookup("closedLid.duration") }
    public static var closedLidDuration30m: String   { lookup("closedLid.duration.30m") }
    public static var closedLidDuration1h: String    { lookup("closedLid.duration.1h") }
    public static var closedLidDuration4h: String    { lookup("closedLid.duration.4h") }
    public static var closedLidDuration8h: String    { lookup("closedLid.duration.8h") }
    public static var closedLidDurationInf: String   { lookup("closedLid.duration.inf") }
    public static var closedLidStateOff: String      { lookup("closedLid.state.off") }
    public static var closedLidStateOnInf: String    { lookup("closedLid.state.onInf") }
    public static var closedLidSubtitle: String      { lookup("closedLid.subtitle") }
    public static func closedLidStateOnTimer(_ time: String) -> String {
        String(format: lookup("closedLid.state.onTimer"), time)
    }

    // MARK: keep awake
    public static var keepAwakeStateOn: String       { lookup("keepAwake.state.on") }
    public static var keepAwakeStateOff: String      { lookup("keepAwake.state.off") }
    public static var keepAwakeStateExternal: String { lookup("keepAwake.state.external") }
    public static var keepAwakeStateBoth: String     { lookup("keepAwake.state.both") }
    public static func keepAwakeExternalPrefix(_ list: String) -> String {
        String(format: lookup("keepAwake.externalPrefix"), list)
    }

    // MARK: settings
    public static var settingsOpenAtLogin: String              { lookup("settings.openAtLogin") }
    public static var settingsOpenAtLoginUnavailable: String   { lookup("settings.openAtLogin.unavailable") }
    public static var settingsClosedLidSection: String         { lookup("settings.closedLidSection") }
    public static var settingsKeepDisplayAwake: String         { lookup("settings.keepDisplayAwake") }
    public static var settingsPreventScreenSaver: String       { lookup("settings.preventScreenSaver") }
    public static var settingsLanguage: String                 { lookup("settings.language") }
    public static var settingsLanguageAuto: String             { lookup("settings.language.auto") }
    public static var settingsLanguageEn: String               { lookup("settings.language.en") }
    public static var settingsLanguageKo: String               { lookup("settings.language.ko") }
    public static var settingsRelaunchTitle: String            { lookup("settings.relaunch.title") }
    public static var settingsRelaunchBody: String             { lookup("settings.relaunch.body") }
    public static var settingsRelaunchConfirm: String          { lookup("settings.relaunch.confirm") }
    public static var settingsRelaunchCancel: String           { lookup("settings.relaunch.cancel") }

    // MARK: error / tooltip / app
    public static var errorTmuxNotConnected: String { lookup("error.tmuxNotConnected") }
    public static var tooltipIdle: String           { lookup("tooltip.idle") }
    public static var tooltipKeepAwake: String      { lookup("tooltip.keepAwake") }
    public static var tooltipClosedLid: String      { lookup("tooltip.closedLid") }
    public static var appName: String               { lookup("app.name") }

    // MARK: helper

    /// 매번 lookup — 언어 변경 후 재시작 시 새 lproj 가 적절히 적용되도록 cache 안 함.
    private static func lookup(_ key: String) -> String {
        NSLocalizedString(key, bundle: .module, comment: "")
    }
}
```

- [ ] **Step 3: I18nTests.swift 작성 — 키 양쪽 lproj 일치 검증**

`Tests/CoreTests/I18nTests.swift` (신규):

```swift
import XCTest
@testable import Core

final class I18nTests: XCTestCase {
    /// 양쪽 .lproj/Localizable.strings 의 키 set 이 정확히 일치해야 함.
    func test_allKeys_existInBothLprojs() throws {
        let bundle = Bundle.module

        let enKeys = try keysFromLproj(in: bundle, lang: "en")
        let koKeys = try keysFromLproj(in: bundle, lang: "ko")

        let onlyInEn = enKeys.subtracting(koKeys)
        let onlyInKo = koKeys.subtracting(enKeys)

        XCTAssertTrue(onlyInEn.isEmpty, "ko.lproj 누락 키: \(onlyInEn.sorted())")
        XCTAssertTrue(onlyInKo.isEmpty, "en.lproj 누락 키: \(onlyInKo.sorted())")
    }

    func test_lookup_returnsNonEmptyForKnownKey() {
        XCTAssertFalse(L.menuKeepAwake.isEmpty)
        XCTAssertFalse(L.closedLidStateOff.isEmpty)
    }

    /// `closedLid.state.onTimer` 의 %@ format substitution 동작.
    func test_closedLidStateOnTimer_substitutesPlaceholder() {
        let s = L.closedLidStateOnTimer("3:47:12")
        XCTAssertTrue(s.contains("3:47:12"), "결과: \(s)")
    }

    // MARK: helpers

    private func keysFromLproj(in bundle: Bundle, lang: String) throws -> Set<String> {
        guard let url = bundle.url(forResource: "Localizable", withExtension: "strings",
                                    subdirectory: nil, localization: lang)
        else {
            XCTFail("\(lang).lproj/Localizable.strings 못 찾음")
            return []
        }
        guard let dict = NSDictionary(contentsOf: url) as? [String: String] else {
            XCTFail("\(lang).lproj/Localizable.strings parse 실패")
            return []
        }
        return Set(dict.keys)
    }
}
```

- [ ] **Step 4: 빌드 + 테스트 통과 확인**

```bash
swift build -c release 2>&1 | tail -3
swift test --filter I18nTests 2>&1 | tail -10
```
Expected: 빌드 OK. XCTest 가능 시 3 tests pass. XCTest 불가 환경이면 빌드만 OK 면 통과로 간주.

- [ ] **Step 5: 커밋**

```bash
git add Sources/Core/Resources/ko.lproj/Localizable.strings Sources/Core/I18n.swift Tests/CoreTests/I18nTests.swift
git commit -m "$(cat <<'EOF'
feat(Core): ko.lproj + enum L 타입세이프 lookup + 키 일치 테스트

ko.lproj/Localizable.strings 한국어 번역 추가. enum L 이 ~50 키를 IDE
자동완성 + 오타 방지 가능하게 노출. lookup 매번 NSLocalizedString —
재시작 후 새 lproj 적용 보장 (cache 안 함).

테스트: en/ko key set 정확히 일치 + sample lookup + format substitution.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: LocaleService — preference + applyAtLaunch + applyAndRelaunch

**Files:**
- Modify: `Sources/Core/I18n.swift` (LanguagePreference + LocaleService 추가)
- Modify: `Tests/CoreTests/I18nTests.swift` (LocaleService 테스트 추가)

목적: 사용자 언어 선택을 UserDefaults 에 저장 + AppleLanguages override + 자동 재시작.

- [ ] **Step 1: I18n.swift 에 LanguagePreference + LocaleService 추가**

`Sources/Core/I18n.swift` 의 `enum L { ... }` 다음에 추가:

```swift
/// Settings 의 Language picker 모델.
public enum LanguagePreference: String, Codable, CaseIterable, Identifiable, Sendable {
    case auto
    case en
    case ko
    public var id: String { rawValue }
}

/// 언어 preference 저장 + AppleLanguages override + relaunch.
@MainActor
public final class LocaleService: ObservableObject {
    public static let key = "muxbar.language"
    private static let appleLanguagesKey = "AppleLanguages"

    @Published public var preference: LanguagePreference {
        didSet { defaults.set(preference.rawValue, forKey: Self.key) }
    }
    private let defaults: UserDefaults

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        let raw = defaults.string(forKey: Self.key) ?? LanguagePreference.auto.rawValue
        self.preference = LanguagePreference(rawValue: raw) ?? .auto
    }

    /// 앱 시작 시 호출. preference 따라 AppleLanguages 결정.
    /// .auto 면 시스템 그대로 (AppleLanguages key 제거), 나머진 forced override.
    public func applyAtLaunch() {
        switch preference {
        case .auto:
            defaults.removeObject(forKey: Self.appleLanguagesKey)
        case .en:
            defaults.set(["en"], forKey: Self.appleLanguagesKey)
        case .ko:
            defaults.set(["ko"], forKey: Self.appleLanguagesKey)
        }
    }

    /// Settings 에서 변경 후 호출 — preference 적용 + 새 인스턴스 띄우고 현재 종료.
    public func applyAndRelaunch() {
        applyAtLaunch()
        let url = Bundle.main.bundleURL
        let config = NSWorkspace.OpenConfiguration()
        config.createsNewApplicationInstance = true
        NSWorkspace.shared.openApplication(at: url, configuration: config) { _, _ in
            DispatchQueue.main.async { NSApp.terminate(nil) }
        }
    }
}
```

`NSWorkspace`, `NSApp` 사용 위해 `import AppKit` 추가. 파일 상단:

```swift
import Foundation
import AppKit
```

- [ ] **Step 2: I18nTests.swift 에 LocaleService 테스트 추가**

기존 `I18nTests` 클래스 안에 추가:

```swift
private func makeIsolatedDefaults() -> UserDefaults {
    let suite = "muxbar.test.locale.\(UUID().uuidString)"
    return UserDefaults(suiteName: suite)!
}

@MainActor
func test_languagePreference_defaultsToAuto() {
    let svc = LocaleService(defaults: makeIsolatedDefaults())
    XCTAssertEqual(svc.preference, .auto)
}

@MainActor
func test_languagePreference_persists() {
    let defaults = makeIsolatedDefaults()
    let svc1 = LocaleService(defaults: defaults)
    svc1.preference = .ko

    let svc2 = LocaleService(defaults: defaults)
    XCTAssertEqual(svc2.preference, .ko)
}

@MainActor
func test_applyAtLaunch_auto_removesAppleLanguagesKey() {
    let defaults = makeIsolatedDefaults()
    defaults.set(["en"], forKey: "AppleLanguages")
    let svc = LocaleService(defaults: defaults)
    svc.preference = .auto
    svc.applyAtLaunch()
    XCTAssertNil(defaults.array(forKey: "AppleLanguages"))
}

@MainActor
func test_applyAtLaunch_en_setsEnglishOverride() {
    let defaults = makeIsolatedDefaults()
    let svc = LocaleService(defaults: defaults)
    svc.preference = .en
    svc.applyAtLaunch()
    XCTAssertEqual(defaults.array(forKey: "AppleLanguages") as? [String], ["en"])
}

@MainActor
func test_applyAtLaunch_ko_setsKoreanOverride() {
    let defaults = makeIsolatedDefaults()
    let svc = LocaleService(defaults: defaults)
    svc.preference = .ko
    svc.applyAtLaunch()
    XCTAssertEqual(defaults.array(forKey: "AppleLanguages") as? [String], ["ko"])
}
```

- [ ] **Step 3: 빌드 + 테스트 통과 확인**

```bash
swift build -c release 2>&1 | tail -3
swift test --filter I18nTests 2>&1 | tail -15
```
Expected: 빌드 OK, XCTest 가능 시 8 tests pass.

- [ ] **Step 4: 커밋**

```bash
git add Sources/Core/I18n.swift Tests/CoreTests/I18nTests.swift
git commit -m "$(cat <<'EOF'
feat(Core): LocaleService — preference 저장 + AppleLanguages override + relaunch

LanguagePreference (auto/en/ko) UserDefaults 영구 저장. applyAtLaunch 가
AppleLanguages key 를 preference 따라 set/remove. applyAndRelaunch 는
NSWorkspace.openApplication 으로 새 인스턴스 띄우고 NSApp.terminate.

테스트 5건: 디폴트 auto / persistence / auto/en/ko 각 applyAtLaunch 동작.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: AppState wiring + MuxBarApp 의 applyAtLaunch 호출

**Files:**
- Modify: `Sources/MuxBarApp/AppState.swift`
- Modify: `Sources/MuxBarApp/MuxBarApp.swift`

목적: AppState 가 LocaleService instance 보유. MuxBarApp init 의 가장 이른 시점에 applyAtLaunch 호출 (UI 가 어떤 string lookup 도 하기 전).

- [ ] **Step 1: AppState 에 localeService 추가**

`Sources/MuxBarApp/AppState.swift` 의 클래스 properties 부분에 추가 (다른 store 들 옆):

```swift
public let localeService: LocaleService
```

`init()` 내에서 초기화 (다른 store 초기화 옆):

```swift
self.localeService = LocaleService()
```

- [ ] **Step 2: MuxBarApp init 에서 applyAtLaunch 호출**

`Sources/MuxBarApp/MuxBarApp.swift` 의 `init()` 수정:

```swift
init() {
    MuxLogging.bootstrap()
    MuxLogging.logger("app").info("muxbar launched")
    // 가장 이른 시점에 AppleLanguages 결정 — 이후 모든 NSLocalizedString lookup 이 적절한 lproj 사용.
    LocaleService().applyAtLaunch()
}
```

`LocaleService()` 를 init 안에서 임시 인스턴스로 호출 (자체 UserDefaults 읽음). `appState.localeService` 가 같은 preference 를 보유하지만 init 시점엔 `@StateObject` wrappedValue 접근 어려움 — 임시 인스턴스로 안전하게.

- [ ] **Step 3: 빌드 + 재기동 확인**

```bash
./build.sh 2>&1 | tail -3
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
sleep 2
pgrep -lf "muxbar.app/Contents/MacOS/muxbar"
cat ~/Library/Logs/muxbar.run.log | head -5
```
Expected: 정상 부팅 (메뉴는 아직 영어 그대로 — string 치환은 Task 5+ 에서).

- [ ] **Step 4: 커밋**

```bash
git add Sources/MuxBarApp/AppState.swift Sources/MuxBarApp/MuxBarApp.swift
git commit -m "$(cat <<'EOF'
feat(MuxBarApp): LocaleService wiring + applyAtLaunch 가장 이른 시점 호출

AppState 에 localeService 인스턴스 추가. MuxBarApp init 안에서 임시
LocaleService 로 applyAtLaunch 호출 — UI 가 어떤 string lookup 도 하기
전에 AppleLanguages 결정 보장.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: SettingsMenu 의 Language picker + 기존 string 치환

**Files:**
- Modify: `Sources/Features/Settings/SettingsMenu.swift`
- Modify: `Sources/MuxBarApp/MuxBarApp.swift` (SettingsMenu 호출에 localeService 추가)

목적: Settings 의 모든 string 을 L.x 로 치환 + Language picker 추가 + alert + 자동 재시작 wiring.

- [ ] **Step 1: SettingsMenu.swift 전체 갱신**

`Sources/Features/Settings/SettingsMenu.swift`:

```swift
import SwiftUI
import Core

public struct SettingsMenu: View {
    @ObservedObject public var loginItemService: LoginItemService
    @ObservedObject public var closedLidPreferences: ClosedLidPreferences
    @ObservedObject public var localeService: LocaleService

    @State private var showingRelaunchAlert = false
    @State private var pendingPreference: LanguagePreference?

    public init(
        loginItemService: LoginItemService,
        closedLidPreferences: ClosedLidPreferences,
        localeService: LocaleService
    ) {
        self.loginItemService = loginItemService
        self.closedLidPreferences = closedLidPreferences
        self.localeService = localeService
    }

    public var body: some View {
        Menu {
            if loginItemService.isAvailable {
                Toggle(isOn: Binding(
                    get: { loginItemService.isEnabled },
                    set: { loginItemService.set($0) }
                )) {
                    Text(L.settingsOpenAtLogin)
                }
            } else {
                Button {} label: {
                    Text(L.settingsOpenAtLoginUnavailable)
                }
                .disabled(true)
            }

            Divider()
            Text(L.settingsClosedLidSection).font(.caption).foregroundStyle(.secondary)
            Toggle(isOn: $closedLidPreferences.keepDisplayAwake) {
                Text(L.settingsKeepDisplayAwake)
            }
            Toggle(isOn: $closedLidPreferences.preventScreenSaver) {
                Text(L.settingsPreventScreenSaver)
            }

            Divider()
            Picker(selection: Binding(
                get: { localeService.preference },
                set: { newValue in
                    pendingPreference = newValue
                    showingRelaunchAlert = true
                }
            )) {
                Text(L.settingsLanguageAuto).tag(LanguagePreference.auto)
                Text(L.settingsLanguageEn).tag(LanguagePreference.en)
                Text(L.settingsLanguageKo).tag(LanguagePreference.ko)
            } label: {
                Text(L.settingsLanguage)
            }
        } label: {
            HStack(spacing: 6) {
                Image(systemName: "gearshape")
                Text(L.menuSettings)
                Spacer()
                Image(systemName: "chevron.right")
                    .font(.caption2)
                    .foregroundStyle(.secondary)
            }
        }
        .menuStyle(.borderlessButton)
        .alert(L.settingsRelaunchTitle, isPresented: $showingRelaunchAlert) {
            Button(L.settingsRelaunchConfirm) {
                if let p = pendingPreference {
                    localeService.preference = p
                    localeService.applyAndRelaunch()
                }
                pendingPreference = nil
            }
            Button(L.settingsRelaunchCancel, role: .cancel) {
                pendingPreference = nil
            }
        } message: {
            Text(L.settingsRelaunchBody)
        }
    }
}
```

- [ ] **Step 2: MuxBarApp.swift 의 SettingsMenu 호출에 localeService 추가**

기존 `SettingsMenu(...)` 호출 (line 93~96 근처):

```swift
SettingsMenu(
    loginItemService: appState.loginItemService,
    closedLidPreferences: appState.closedLidPreferences,
    localeService: appState.localeService
)
.padding(.horizontal, 8)
.padding(.vertical, 6)
```

- [ ] **Step 3: 빌드 + 재기동 + manual 확인**

```bash
./build.sh 2>&1 | tail -3
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
```

메뉴 → Settings → Language picker 표시 + Auto/English/한국어 선택 가능 + 변경 시 alert 뜸.
(Restart 까지는 manual; 사용자가 confirm 시 새 인스턴스 띄움)

- [ ] **Step 4: 커밋**

```bash
git add Sources/Features/Settings/SettingsMenu.swift Sources/MuxBarApp/MuxBarApp.swift
git commit -m "$(cat <<'EOF'
feat(Settings): Language picker + 기존 string 을 L.x lookup 으로 치환

Auto / English / 한국어 picker. 선택 변경 시 alert (Restart? / Cancel) 띄우고
Restart 클릭 시 LocaleService.applyAndRelaunch 호출 → 새 인스턴스 띄우고
현재 종료. Cancel 시 변경 취소.

Open at Login / Closed-lid section / Keep display awake / Prevent screen
saver / Settings 라벨 모두 L.x 로 치환.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: KeepAwakeMenuItem string 치환

**Files:**
- Modify: `Sources/Features/KeepAwake/KeepAwakeMenuItem.swift`

목적: Keep Awake 라벨 + 상태 (`OFF`/`ON`/`ON (external)`/`ON (muxbar + external)`) + external 리스트 prefix 를 L.x 로.

- [ ] **Step 1: KeepAwakeMenuItem.swift 수정**

기존 `Text("Keep Awake")` → `Text(L.menuKeepAwake)`.
기존 `Text(source.label)` 가 hardcoded → 별 helper 로 L lookup.

`source.label` 이 `AwakeStore.Source` enum 의 computed property 라면 view-side 에서 L lookup helper 추가:

```swift
private func localizedLabel(_ source: AwakeStore.Source) -> String {
    switch source {
    case .none: return L.keepAwakeStateOff
    case .muxbar: return L.keepAwakeStateOn
    case .external: return L.keepAwakeStateExternal
    case .both: return L.keepAwakeStateBoth
    }
}
```

`Text(source.label)` → `Text(localizedLabel(source))`.

`Text("external: \(externalSessionList)")` → `Text(L.keepAwakeExternalPrefix(externalSessionList))`.

- [ ] **Step 2: 빌드 확인**

```bash
swift build -c release 2>&1 | tail -3
```

- [ ] **Step 3: 커밋**

```bash
git add Sources/Features/KeepAwake/KeepAwakeMenuItem.swift
git commit -m "$(cat <<'EOF'
i18n(KeepAwake): 메뉴 라벨 + 상태 라벨 + external prefix 를 L.x lookup

source.label 의 hardcoded "OFF/ON/..." 을 view-local localizedLabel
helper 로 분리해 L.keepAwakeState* 사용. external session list 도
keepAwakeExternalPrefix 의 %@ format 으로.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: ClosedLidMenuItem string 치환

**Files:**
- Modify: `Sources/Features/ClosedLid/ClosedLidMenuItem.swift`

목적: Closed-lid mode 라벨 + 상태 + duration popover + subtitle 모두 L.x 로.

- [ ] **Step 1: ClosedLidMenuItem.swift 수정**

기존 `Text("Closed-lid mode")` → `Text(L.menuClosedLid)`.
기존 `Text("sleep blocked (system-wide)")` → `Text(L.closedLidSubtitle)`.

`stateLabel` 의 switch:
```swift
private var stateLabel: String {
    switch store.state {
    case .off:
        return L.closedLidStateOff
    case .on(let expiresAt):
        guard let expiresAt else { return L.closedLidStateOnInf }
        let remaining = max(0, Int(expiresAt.timeIntervalSince(now)))
        let h = remaining / 3600
        let m = (remaining % 3600) / 60
        let s = remaining % 60
        let timeStr = String(format: "%d:%02d:%02d", h, m, s)
        return L.closedLidStateOnTimer(timeStr)
    }
}
```

duration popover 의 `Text("Duration")` → `Text(L.closedLidDuration)`.

`durationOptions` 의 label 을 정적 String 에서 closure 로 lazy lookup:
```swift
private static func durationOptions() -> [(label: String, duration: Duration?)] {
    [
        (L.closedLidDuration30m, .seconds(1800)),
        (L.closedLidDuration1h, .seconds(3600)),
        (L.closedLidDuration4h, .seconds(14400)),
        (L.closedLidDuration8h, .seconds(28800)),
        (L.closedLidDurationInf, nil),
    ]
}
```
호출부 `ForEach(Self.durationOptions, ...)` → `ForEach(Self.durationOptions(), ...)`.

(static let 으로 두면 첫 lookup 시점에 cache 되어 언어 변경 후 재시작 전까진 영어 유지 — 우리 흐름은 변경 즉시 재시작이라 OK 이지만, function 으로 매번 lookup 이 더 안전.)

- [ ] **Step 2: 빌드 확인**

```bash
swift build -c release 2>&1 | tail -3
```

- [ ] **Step 3: 커밋**

```bash
git add Sources/Features/ClosedLid/ClosedLidMenuItem.swift
git commit -m "$(cat <<'EOF'
i18n(ClosedLid): 메뉴 라벨 / 상태 / duration popover / subtitle 치환

stateLabel 의 OFF/ON·∞/ON·counter 모두 L.closedLidState* 사용. duration
popover 의 "Duration" 라벨 + 30m/1h/4h/8h/∞ 옵션 라벨 모두 L.closedLid*
lookup. options 를 static let → static func 으로 변경 — lookup 매번 발생.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 세션 리스트 + 템플릿 메뉴 string 치환

**Files:**
- Modify: `Sources/Features/SessionList/SessionRowView.swift`
- Modify: `Sources/Features/Templates/NewSessionMenu.swift`

목적: 세션 행의 Attach/Preview/Kill 버튼 + New Session 메뉴.

- [ ] **Step 1: SessionRowView.swift 의 버튼 라벨 치환**

기존:
```swift
Button("Attach") { onAttach() }
Button("Preview") { onPreview() }
Button("Kill", role: .destructive) { onKill() }
```

→ 변경:
```swift
Button(L.menuAttach) { onAttach() }
Button(L.menuPreview) { onPreview() }
Button(L.menuKill, role: .destructive) { onKill() }
```

`import Core` 가 없으면 추가.

- [ ] **Step 2: NewSessionMenu.swift 의 라벨 치환**

기존 `Text("New Session")` → `Text(L.menuNewSession)`.
"Edit Templates…" / "Reload Templates" 같은 라벨이 있다면 각각 `L.menuOpenTemplates` / `L.menuReloadTemplates` 로.

(파일 read 후 모든 user-facing string 식별 → 각 L.x 로 치환.)

- [ ] **Step 3: 빌드 확인**

```bash
swift build -c release 2>&1 | tail -3
```

- [ ] **Step 4: 커밋**

```bash
git add Sources/Features/SessionList/SessionRowView.swift Sources/Features/Templates/NewSessionMenu.swift
git commit -m "$(cat <<'EOF'
i18n(SessionList,Templates): Attach/Preview/Kill + New Session/templates 메뉴 치환

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: MuxBarApp 의 header / Quit / tooltip 치환

**Files:**
- Modify: `Sources/MuxBarApp/MuxBarApp.swift`

목적: 메뉴 header 의 "muxbar" 라벨, Quit 버튼, 메뉴바 아이콘 dynamic tooltip.

- [ ] **Step 1: header 의 Text("muxbar") → Text(L.appName)**

```swift
private var header: some View {
    HStack {
        Image(systemName: "terminal.fill")
        Text(L.appName)
            .font(.headline)
        Spacer()
        // ...
    }
    .padding(8)
}
```

- [ ] **Step 2: Quit 버튼 라벨**

```swift
Button(L.menuQuit) { NSApplication.shared.terminate(nil) }
    .keyboardShortcut("q")
    .padding(.horizontal, 8)
    .padding(.vertical, 6)
```

- [ ] **Step 3: menuBarTooltip computed property 치환**

```swift
private var menuBarTooltip: String {
    if appState.closedLidStore.state.isOn {
        return L.tooltipClosedLid
    }
    if appState.awakeStore.isAwake(in: appState.sessionStore) {
        return L.tooltipKeepAwake
    }
    return L.tooltipIdle
}
```

- [ ] **Step 4: AppState 의 에러 메시지 — `error.tmuxNotConnected`**

`Sources/MuxBarApp/AppState.swift` 의 `turnOnClosedLid` 안:

```swift
guard let client = controlClient else {
    sessionStore.apply(error: L.errorTmuxNotConnected)
    return
}
```

(다른 user-facing 에러 메시지가 있으면 모두 L.x 로 — `git grep -n 'sessionStore.apply(error:' Sources/` 로 확인.)

- [ ] **Step 5: 빌드 + 재기동 + manual 확인**

```bash
./build.sh 2>&1 | tail -3
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
```

메뉴바 아이콘 hover → tooltip 표시. 메뉴 열기 → 모든 라벨 정상.

- [ ] **Step 6: 커밋**

```bash
git add Sources/MuxBarApp/MuxBarApp.swift Sources/MuxBarApp/AppState.swift
git commit -m "$(cat <<'EOF'
i18n(MuxBarApp): header / Quit / tooltip / 에러 메시지 L.x lookup

menu header 의 muxbar 라벨, Quit 버튼, 메뉴바 동적 tooltip (idle /
KeepAwake / closed-lid 3 상태), turnOnClosedLid 의 tmux not connected
에러 메시지까지 모두 치환.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Manual 검증 + ko 번역 사람-수정 검수

**Files:**
- Optional Modify: `Sources/Core/Resources/ko.lproj/Localizable.strings` (translation polish)
- Modify: `README.md` / `README.ko.md` (optional)

목적: 실제 한국어 환경에서 시각 검수 + 자연스러운 번역으로 다듬기 + 추가 Release 가 필요하면 README 추가.

- [ ] **Step 1: 한국어 모드로 강제 띄우기**

```bash
defaults write com.1989v.muxbar AppleLanguages -array "ko"
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
```

메뉴바 클릭 → 한국어 라벨 표시 확인.

- [ ] **Step 2: 시각 검수 체크리스트**

각 항목 메뉴에서 직접 확인:

- [ ] header: "muxbar" + 연결 dot
- [ ] 세션 행: Attach / 미리보기 / 종료 (action menu)
- [ ] Keep Awake 라벨 + 상태 (OFF / ON / ON (외부) / ON (muxbar + 외부))
- [ ] Closed-lid mode 라벨 + 상태 (OFF / ON · ∞ / ON · 카운트다운) + subtitle
- [ ] Closed-lid duration popover (지속 시간 + 30분/1시간/4시간/8시간/∞)
- [ ] New Session 메뉴
- [ ] Settings → 시작 시 자동 실행 / Closed-lid mode / 화면 켜둠 / 화면 보호기 차단 / 언어 picker / 재시작 alert
- [ ] muxbar 종료 (Quit 버튼)
- [ ] 메뉴바 아이콘 hover tooltip — 한국어 메시지

어색한 표현 발견 시 ko.lproj 의 해당 키 polish 후 빌드 + 재시작 후 재확인.

- [ ] **Step 3: 영어 모드로 돌려놓기 + 동일 검수**

```bash
defaults write com.1989v.muxbar AppleLanguages -array "en"
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
```

위 체크리스트 동일하게 영어에서 검수.

- [ ] **Step 4: Auto 모드 검수 (시스템 locale 따름)**

```bash
defaults delete com.1989v.muxbar AppleLanguages 2>/dev/null
defaults delete com.1989v.muxbar muxbar.language 2>/dev/null
pkill -f "muxbar.app/Contents/MacOS/muxbar"; sleep 1
nohup ./muxbar.app/Contents/MacOS/muxbar > ~/Library/Logs/muxbar.run.log 2>&1 & disown
```

시스템 locale (`defaults read -g AppleLanguages`) 가 ko 면 한국어, en 이면 영어 표시 확인.

- [ ] **Step 5: 번역 polish 발견 시 commit**

```bash
git add Sources/Core/Resources/ko.lproj/Localizable.strings
git commit -m "$(cat <<'EOF'
i18n(ko): 사람-검수 후 자연스러운 한국어 표현 polish

(어떤 키들 다듬었는지 본문에 짧게 — 발견된 항목만)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(polish 없으면 이 step skip.)

- [ ] **Step 6: README 항목 추가 (선택)**

`README.md` Features 섹션에 한 줄 추가:

```markdown
- **Multilingual** — English + 한국어. Settings → Language to switch (Auto / English / 한국어).
```

`README.ko.md` 에:

```markdown
- **다국어 지원** — English + 한국어. Settings → 언어 에서 전환 (자동 / English / 한국어).
```

```bash
git add README.md README.ko.md
git commit -m "$(cat <<'EOF'
docs: i18n 항목 README 추가 (en/ko)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review Notes

**Spec coverage 체크 (`docs/specs/2026-05-09-i18n-design.md` 의 §):**

- §1.2 Goals
  - G1 macOS system locale 자동 감지 → Task 3 (LocaleService.applyAtLaunch `.auto`) + Task 4 (init 호출) ✓
  - G2 Settings picker → Task 5 ✓
  - G3 자동 재시작 → Task 5 (alert + applyAndRelaunch) + Task 3 ✓
  - G4 추가 언어 확장 가능 → 인프라 (Task 1-2) 가 .lproj 추가만으로 활성화 ✓
  - G5 fallback → 컴파일 시 NSLocalizedString 의 Apple 내장 fallback (en → key) ✓
- §3.1 Localizable.strings → Task 1, 2 ✓
- §3.2 I18n.swift (enum L + LanguagePreference + LocaleService) → Task 2, 3 ✓
- §3.3 Package.swift resources → Task 1 ✓
- §3.4 SettingsMenu Language picker → Task 5 ✓
- §3.5 AppState/MuxBarApp wiring → Task 4 ✓
- §4 Data Flow → Task 5 의 alert + applyAndRelaunch 로 구현 ✓
- §6 Settings UI 표시 → Task 5 ✓
- §7 Testing — unit (Task 2, 3) + manual (Task 10) ✓
- §8 Out of Scope — 추가 언어/플루랄/RTL 모두 본 plan 에서 제외 ✓

**Type 일관성:**
- `LanguagePreference` 의 case 이름 `.auto/.en/.ko` 모든 task 동일 ✓
- `LocaleService.preference` property 이름 일관 ✓
- `L.menuKeepAwake` 등 키 이름 모든 task 동일 (Localizable.strings 의 `menu.keepAwake` 와 매핑) ✓
- `Bundle.module` 사용 일관 ✓

**Placeholder:** 없음.
